# ## 3. 
# # Compute the area of a circle with radius 7, stored into a variable called area. Print a nice message with the answer.
# # Note- Search for the formula to calculate area of a circle.

# #Solution:
from math import pi

radius = 7
Area = pi * radius**2
print(round(Area))